# docs/

Project documentation workspace — the pkgdown site, Quarto vignettes, and (from
Phase 9) the AI-readable docs bundle (`llms.txt`). Built from **WP8** (release)
onward.

The binding planning documents live at the repository root
(`SOLO-PHASE-PLAN.md`, `ROADMAP.md`, `API-GRAMMAR.md`, `ARCHITECTURE.md`,
`DECISIONS.md`), not here.

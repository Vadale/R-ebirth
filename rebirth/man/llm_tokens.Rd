% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tokens.R
\name{llm_tokens}
\alias{llm_tokens}
\title{Tokenize text, or decode token ids back to text}
\usage{
llm_tokens(m, x, decode = FALSE)
}
\arguments{
\item{m}{An \code{llm} handle.}

\item{x}{For encoding, a character vector; for decoding, an integer vector of
1-based token ids.}

\item{decode}{Single logical. \code{FALSE} (default) encodes text to ids; \code{TRUE}
decodes ids to text.}
}
\value{
Encoding: a named integer vector (single input) or a list of them
(several inputs). Decoding: a single string.
}
\description{
Converts between text and the model's tokens. With \code{decode = FALSE} (the
default) \code{x} is text and the result is the token ids; with \code{decode = TRUE}
\code{x} is token ids and the result is the reconstructed string.
}
\details{
\strong{Encoding} (\code{decode = FALSE}). \code{x} is a character vector. Each element is
tokenized into a \strong{named integer vector}: the values are the token ids and
the names are the token pieces (the text each id renders as). For a single
string a named integer vector is returned; for several strings a list of such
vectors is returned, preserving \code{names(x)}. No beginning/end-of-sequence
markers are added (you get exactly the tokens of the text); chat formatting is
\code{llm_generate}'s job, not this function's.

\strong{Decoding} (\code{decode = TRUE}). \code{x} is an integer vector of token ids and the
result is a single string. Decoding is UTF-8 correct even when a multi-byte
character spans two tokens — always decode the whole id vector rather than
concatenating the piece names, which can split a character.

Token ids are \strong{1-based} in the R API (like every other index in \code{rebirth});
subtract 1 to compare them with a raw vocabulary index (llama.cpp / Hugging
Face). Encoding and decoding are exact inverses:
\code{llm_tokens(m, llm_tokens(m, txt), decode = TRUE)} reproduces \code{txt}.

The model must carry a tokenizer; a \code{no_vocab} model raises
\code{rebirth_error_tokenize}.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("REBIRTH_TEST_MODEL_QWEN"))) withAutoprint(\{ # examplesIf}
m <- llm(Sys.getenv("REBIRTH_TEST_MODEL_QWEN"))
ids <- llm_tokens(m, "The quick brown fox")
ids
llm_tokens(m, ids, decode = TRUE)
close(m)
\dontshow{\}) # examplesIf}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed.R
\name{llm_embed}
\alias{llm_embed}
\title{Embed text with a model}
\usage{
llm_embed(m, x, pooling = c("mean", "last", "model"), normalize = TRUE)
}
\arguments{
\item{m}{An \code{llm} handle from \code{\link[=llm]{llm()}}.}

\item{x}{A non-empty character vector to embed; \code{names(x)} become the row names.}

\item{pooling}{How to reduce each input's per-token vectors to one vector:
\code{"mean"} (average), \code{"last"} (final token), or \code{"model"} (the model's own
pooling when the GGUF defines one; otherwise an error asking for
\code{"mean"}/\code{"last"}).}

\item{normalize}{Single logical. \code{TRUE} (default) L2-normalizes each row so
rows are unit vectors and dot products are cosine similarities.}
}
\value{
A numeric \code{matrix}, \code{length(x)} rows by the model's embedding size
(columns), with row names \code{names(x)} when set, else the input positions as
characters.
}
\description{
Encodes each string in \code{x} into a fixed-length numeric vector, returning a base
\code{matrix} with one row per input and one column per embedding dimension (so the
matrix is \code{length(x)} by the model's \code{hidden_size}).
}
\details{
Each input is tokenized (with the model's begin/end-of-sequence markers) and run
through a forward pass in a dedicated embeddings-mode context; the per-token
hidden states are then pooled into one vector per input by \code{pooling}. \code{"mean"}
averages the token vectors, \code{"last"} takes the final token's vector, and
\code{"model"} uses the model's own pooling when the GGUF defines one (a purely
generative model such as Qwen2.5 defines none, so \code{"model"} raises
\code{rebirth_error_embed} asking for \code{"mean"} or \code{"last"}).

With \code{normalize = TRUE} (the default) each row is L2-normalized to a unit
vector, so the dot product of two rows is their cosine similarity. A zero vector
is returned unchanged (never \code{NaN}). \code{normalize} is validated and its effect is
explicit — there is no silent normalization.

The model must carry a tokenizer; a \code{no_vocab} model raises
\code{rebirth_error_tokenize}. No model ships inside the package yet (the in-repo
synthetic model has no tokenizer), so the runnable example is guarded by the
\code{REBIRTH_TEST_MODEL_QWEN} environment variable — point it at a local Qwen2.5
GGUF to run it.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("REBIRTH_TEST_MODEL_QWEN"))) withAutoprint(\{ # examplesIf}
m <- llm(Sys.getenv("REBIRTH_TEST_MODEL_QWEN"))
e <- llm_embed(m, c(a = "cats and dogs", b = "domestic pets"))
dim(e)
close(m)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=llm]{llm()}}, \code{\link[=llm_tokens]{llm_tokens()}}, \code{\link[=llm_generate]{llm_generate()}}
}

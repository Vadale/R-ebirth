% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logits.R
\name{llm_logits}
\alias{llm_logits}
\title{Inspect the next-token distribution}
\usage{
llm_logits(m, prompt, top = 20)
}
\arguments{
\item{m}{An \code{llm} handle from \code{\link[=llm]{llm()}}.}

\item{prompt}{A character vector of prompts; the result has \code{top} rows per
prompt, and \code{prompt_id} is the 1-based index into \code{prompt}.}

\item{top}{Single positive integer: how many top candidates to return per prompt
(clamped to the vocabulary size). Default 20.}
}
\value{
A base \code{data.frame} with \code{top} rows per prompt and columns
\code{prompt_id} (int, 1-based index into \code{prompt}), \code{rank} (int, 1 = most likely),
\code{token_id} (int, 1-based), \code{token} (chr, the token piece), \code{logit} (dbl), and
\code{prob} (dbl, softmax over the full vocabulary).
}
\description{
Runs a forward pass over each \code{prompt} and returns the model's distribution for
the single token that would come next: the \code{top} most likely candidates, ranked
from most to least probable. This is the raw material behind generation — the
first token \code{\link[=llm_generate]{llm_generate()}} would pick (greedily) is exactly this table's
rank-1 token.
}
\details{
Each prompt is treated as raw text (no chat template is applied — wrap it
yourself if you want the chat framing) and tokenized the way \code{chat = FALSE}
generation is. The distribution is read at the final position, so it answers
"what token does the model expect immediately after this text?". Probabilities
are the softmax over the \strong{whole} vocabulary, computed before the \code{top}
candidates are selected, so each \code{prob} is the token's true share of the full
distribution: the returned probabilities are correct but (being only the head of
the distribution) sum to less than 1.

The result is one long-format \code{data.frame} with \code{top} rows per prompt, stacked
in prompt order. Within each prompt the rows are ordered by descending logit,
so \code{rank == 1} is the most likely next token; \code{logit} and \code{prob} are therefore
non-increasing down the ranks. Token ids are \strong{1-based} (like every index in
\code{relm}, and like \code{\link[=llm_tokens]{llm_tokens()}}), so \code{token_id} round-trips through
\code{llm_tokens(m, token_id, decode = TRUE)}.

Active interventions on \code{m} (from \code{\link[=llm_steer]{llm_steer()}}/\code{\link[=llm_ablate]{llm_ablate()}}) apply, so
\code{llm_logits()} is the direct way to read how a steer or an ablation reshapes the
next-token distribution.

The model must carry a tokenizer; a \code{no_vocab} model (such as the in-repo
synthetic model) raises \code{relm_error_tokenize}. No model ships inside the
package yet, so the runnable example is guarded by the \code{RELM_TEST_MODEL_QWEN}
environment variable — point it at a local Qwen2.5 GGUF to run it.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("RELM_TEST_MODEL_QWEN"))) withAutoprint(\{ # examplesIf}
m <- llm(Sys.getenv("RELM_TEST_MODEL_QWEN"))
llm_logits(m, "The capital of France is", top = 5)
close(m)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=llm]{llm()}}, \code{\link[=llm_generate]{llm_generate()}}, \code{\link[=llm_tokens]{llm_tokens()}}
}

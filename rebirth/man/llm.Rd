% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm.R
\name{llm}
\alias{llm}
\alias{print.llm}
\title{Load a local large language model}
\usage{
llm(
  path,
  context_length = 4096,
  gpu_layers = NULL,
  backend = c("auto", "metal", "cuda", "cpu"),
  mmap = TRUE,
  projector = NULL
)

\method{print}{llm}(x, ...)
}
\arguments{
\item{path}{Single string: path to a GGUF model file.}

\item{context_length}{Positive integer: the active context window in tokens
(llama.cpp: \code{n_ctx}). Default 4096.}

\item{gpu_layers}{\code{NULL} (auto: offload every layer that fits) or a single
non-negative integer count of layers to offload (llama.cpp:
\code{n_gpu_layers}). Ignored on the CPU backend.}

\item{backend}{One of \code{"auto"}, \code{"metal"}, \code{"cuda"}, \code{"cpu"}.}

\item{mmap}{Logical: memory-map the model file (default \code{TRUE}).}

\item{projector}{\code{NULL} (default: text-only, unchanged) or a single string:
path to the model's \strong{mmproj GGUF}, enabling image input (see the
\emph{Image input} section).}

\item{x}{An \code{llm} handle.}

\item{...}{Ignored.}
}
\value{
An object of class \code{llm} (see the package's class documentation).

\code{x}, invisibly.
}
\description{
Loads a GGUF model file and returns an \code{llm} handle: an external pointer to
the native model plus its metadata. All arguments are validated in R before
the native boundary is crossed, so a bad request is reported as a classed
condition and never reaches the engine.
}
\details{
The handle owns native memory (potentially several gigabytes). Free it
deterministically with \link[=close.llm]{close()} when done; a garbage-collection
finalizer frees it as a safety net otherwise (see \code{\link[=close.llm]{close.llm()}}).

\code{backend = "auto"} resolves to the fastest backend this build supports
(Metal on Apple silicon, otherwise CPU). Requesting a backend the build was
not compiled with raises \code{relm_error_backend}.

No model ships inside the package yet, so the runnable example is guarded by
the \code{RELM_TEST_MODEL_QWEN} environment variable (point it at a local
Qwen2.5 GGUF to run it). A tiny in-repo model arrives in a later work
package.
}
\section{Image input (vision models)}{

\code{projector} enables image input for a vision-language model: point it at the
model's companion \strong{mmproj GGUF} (the vision projector published alongside
the model, e.g. \verb{mmproj-*.gguf}). The projector is a session property fixed
at load — it is bound to the loaded model — so it belongs on \code{llm()}, not on
each call; a handle loaded with a projector then accepts
\link[=llm_generate]{llm_generate()}'s \code{images} argument. A projector whose input
embedding size does not match the model raises \code{relm_error_image} naming
both sizes.

\strong{Trust note:} the projector file is engine-trusted input, exactly like
\code{path} — a corrupt or hostile GGUF is parsed by native code. Prefer files
whose SHA256 you can verify (e.g. fetched with \code{\link[=llm_download]{llm_download()}}); treat a
projector from an unknown source with the same care as a model file.
}

\examples{
\dontshow{if (nzchar(Sys.getenv("RELM_TEST_MODEL_QWEN"))) withAutoprint(\{ # examplesIf}
m <- llm(Sys.getenv("RELM_TEST_MODEL_QWEN"))
print(m)
summary(m)
close(m)
\dontshow{\}) # examplesIf}
\dontshow{if (nzchar(Sys.getenv("RELM_TEST_MODEL_VLM")) && nzchar(Sys.getenv("RELM_TEST_MMPROJ_VLM"))) withAutoprint(\{ # examplesIf}
# A vision-language model: pass its companion mmproj as the projector
# (e.g. llm_download("qwen2-vl-2b-instruct-q4_k_m") and
# llm_download("qwen2-vl-2b-instruct-mmproj-f16")).
m <- llm(Sys.getenv("RELM_TEST_MODEL_VLM"),
  projector = Sys.getenv("RELM_TEST_MMPROJ_VLM")
)
print(m)
close(m)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=close.llm]{close.llm()}}, \code{\link[=print.llm]{print.llm()}}, \code{\link[=summary.llm]{summary.llm()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{llm_download}
\alias{llm_download}
\title{Download a pinned model, verified by checksum}
\usage{
llm_download(model, dir = NULL, quiet = FALSE)
}
\arguments{
\item{model}{Single string: a registry alias (see Details) or a full
\verb{https://} URL to a GGUF file.}

\item{dir}{\code{NULL} (the user cache directory) or a single string naming the
directory to download into (created recursively if missing). Use a directory
only you can write to; downloading into a world-writable/shared directory is
not supported.}

\item{quiet}{Single logical. \code{TRUE} suppresses the download progress bar and
the informational messages. Default \code{FALSE}.}
}
\value{
The local file path, returned \strong{invisibly}. Errors:
\code{relm_error_argument} (bad \code{model}/\code{dir}/\code{quiet} type),
\code{relm_error_download} (non-HTTPS URL, unknown alias, network failure,
checksum mismatch, or an unwritable directory).
}
\description{
Fetches a GGUF model file over HTTPS into a local cache and returns its path.
\code{model} is either a pinned alias from the package's model registry (see the
Details section) or a full \verb{https://} URL. The download is
\strong{fail-closed}: for a registry alias the file's SHA256 must match the pinned
value or the file is deleted and \code{relm_error_download} is raised. Nothing
downloaded is ever executed — the file is only written to disk and checksummed.
}
\details{
\strong{Registry aliases.} The package ships a small registry (\code{inst/models.csv})
of license-clean, checksum-pinned models. The current aliases are:
\itemize{
\item \code{"qwen2.5-0.5b-instruct-q8_0"} — Qwen2.5 0.5B Instruct, Q8_0 (Apache-2.0);
the small CI-integration model.
\item \code{"qwen2.5-1.5b-instruct-q4_k_m"} — Qwen2.5 1.5B Instruct, Q4_K_M
(Apache-2.0); the demo default.
}

Passing an unknown alias raises \code{relm_error_download} and lists the known
aliases. Larger Qwen quantizations (7B) ship as split multi-part GGUFs and are
not in the single-file registry yet; Gemma models are gated by the Gemma Terms
of Use (they require accepting terms and an access token on Hugging Face), so
they are supplied by local path rather than downloaded here.

\strong{Full URLs.} A full \verb{https://} URL is downloaded as given. Only HTTPS is
accepted (\verb{http://}, \verb{ftp://}, \verb{file://}, ... are rejected). A bare URL has no
pinned checksum, so the file cannot be \emph{verified}; it is downloaded and its
computed SHA256 is reported so you can pin it — the function never presents an
unverifiable file as if it had been verified.

\strong{Caching.} \code{dir = NULL} uses the per-user cache directory
\code{tools::R_user_dir("relm", "cache")}; the directory is created if missing.
The target file name is the URL's last path segment. If a registry model is
already present and its checksum matches, the download is skipped (idempotent,
offline-friendly); a present-but-mismatching file is treated as corrupt and
re-downloaded. A bare URL has no pinned checksum, so a same-named cached file
cannot be shown to have come from that URL and is never reused -- a bare URL is
always re-fetched. Verification happens on a temporary file that is only moved
into place once it passes, so the final path never holds unverified bytes.
Downloads follow HTTPS redirects; for a registry alias the pinned checksum
still guards integrity end-to-end, so a redirect cannot substitute the bytes.

This is one of only two functions in the package that write outside a session
temporary directory (the other is \code{\link[=llm_trace]{llm_trace()}} spill); see the package's
side-effect contract.
}
\examples{
# The default download directory (dir = NULL); no network access is performed:
tools::R_user_dir("relm", "cache")

\dontrun{
# Fetch a pinned, checksum-verified model and load it:
path <- llm_download("qwen2.5-0.5b-instruct-q8_0")
m <- llm(path)
}
}
\seealso{
\code{\link[=llm]{llm()}}
}

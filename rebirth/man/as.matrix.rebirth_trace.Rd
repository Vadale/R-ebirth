% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace.R
\name{as.matrix.rebirth_trace}
\alias{as.matrix.rebirth_trace}
\title{Extract one activation slice as a matrix}
\usage{
\method{as.matrix}{rebirth_trace}(x, layer, component = "residual", ...)
}
\arguments{
\item{x}{A \code{rebirth_trace} from \code{\link[=llm_trace]{llm_trace()}}.}

\item{layer}{Required single 1-based layer index; it must be present in the trace.}

\item{component}{Single component name (default \code{"residual"}); it must be present
in the trace.}

\item{...}{Ignored.}
}
\value{
A numeric \code{matrix}, rows = captured \verb{(prompt_id, token_pos)} (row names
\code{"<prompt_id>.<token_pos>"}), columns = neurons.
}
\description{
Pulls a single \verb{(layer, component)} slice out of a \code{\link[=llm_trace]{llm_trace()}} result as a
base numeric \code{matrix}: one row per captured \verb{(prompt_id, token_pos)} and one
column per neuron (\code{hidden_size} wide). Row names are \code{"<prompt_id>.<token_pos>"}.
This is the bridge from the long-format trace to matrix tools such as
\code{\link[stats:prcomp]{stats::prcomp()}}.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("REBIRTH_TEST_MODEL_QWEN"))) withAutoprint(\{ # examplesIf}
m <- llm(Sys.getenv("REBIRTH_TEST_MODEL_QWEN"))
tr <- llm_trace(m, "The cat sat.", layers = 1:2)
as.matrix(tr, layer = 1, component = "residual")
close(m)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=llm_trace]{llm_trace()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervene.R
\name{llm_steer}
\alias{llm_steer}
\title{Steer a model along a direction}
\usage{
llm_steer(m, layer, direction, coef = 1, positions = "all")
}
\arguments{
\item{m}{An \code{llm} handle from \code{\link[=llm]{llm()}}.}

\item{layer}{Single 1-based transformer block to steer, in \code{2:m$layers} (layer
1 is not steerable -- see Details).}

\item{direction}{Numeric vector of length \code{m$hidden_size}: the steering
direction in residual space (finite, no \code{NA}).}

\item{coef}{Single finite number scaling \code{direction} (default \code{1}). Negative
values steer the opposite way.}

\item{positions}{Which token positions to steer. Only \code{"all"} (the default) is
supported in this release.}
}
\value{
A new \code{llm} handle with the steering added to its accumulated
\code{interventions}; the source handle is returned unchanged. \code{print()} shows the
active intervention count and \code{summary()} lists them.
}
\description{
Returns a \strong{new} \code{llm} handle that adds \code{coef * direction} to the residual
stream at \code{layer} during every subsequent forward pass (\code{llm_generate()},
\code{llm_logits()}). The original handle is untouched: to remove the steering, use
the original object (reversibility is exact, D-003). Interventions compose --
see Details.
}
\details{
Steering is llama.cpp's native control-vector mechanism: a per-layer,
\code{hidden_size}-wide vector added to the residual at \code{layer}'s output, for \strong{all
token positions} (hence \code{positions = "all"} is the only supported value in this
release; a position subset raises \code{rebirth_error_intervention}).

\strong{Composition.} Deriving from an already-steered or ablated handle carries its
full spec forward and adds the new one, then builds a single fresh context from
the original weights (contexts are never chained). Steering \strong{stacks by
summation} -- two steers on the same layer add -- and the semantics are
\strong{derivation-order-independent}: \code{m |> llm_steer(...) |> llm_ablate(...)}
produces the same forward pass as \code{m |> llm_ablate(...) |> llm_steer(...)}. A
steer never moves an ablated neuron: at the graph the ablation runs after the
steer (\code{(x + steer) * mask + add}), so a jointly steered-and-ablated neuron is
forced to the ablation \code{value} (D-016).

\strong{Layer 1 is not steerable.} The native control vector reserves engine index 0
and has no slot for the first transformer block, so \code{layer = 1} raises
\code{rebirth_error_intervention}. Steer a later layer (\code{2:m$layers}), or ablate
layer 1 with \code{\link[=llm_ablate]{llm_ablate()}} (ablation covers every layer).

\strong{Not free.} Each \code{llm_steer()}/\code{llm_ablate()} call allocates a fresh context
on the shared weights -- a sub-second pause and real memory (its own KV cache,
~hundreds of MB on the demo models), not a cheap copy. Hold a handful of
intervened handles, not hundreds.

\strong{Generation/logits only (for now).} Interventions apply to \code{llm_generate()}
and \code{llm_logits()}. \code{\link[=llm_embed]{llm_embed()}} and \code{\link[=llm_trace]{llm_trace()}} build their own fresh
contexts that do not inherit the adapters, so calling them on an intervened
handle raises \code{rebirth_error_embed} / \code{rebirth_error_trace} rather than silently
returning base (un-intervened) vectors mislabeled as steered.

Supported on the \code{llama}, \code{qwen2}, and \code{gemma3} architectures (the residual
choke point the mechanism hooks); any other architecture raises
\code{rebirth_error_intervention} rather than silently doing nothing.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("REBIRTH_TEST_MODEL_QWEN"))) withAutoprint(\{ # examplesIf}
m <- llm(Sys.getenv("REBIRTH_TEST_MODEL_QWEN"))
# A steering direction is any hidden_size-wide vector (here a placeholder).
dir <- rep(0.05, m$hidden_size)
steered <- llm_steer(m, layer = 8, direction = dir, coef = 2)
summary(steered) # lists the active intervention
# The original handle is untouched -- generation reproduces the base output.
identical(
  llm_generate(m, "Tell me about the sea.", max_tokens = 20, seed = 1),
  llm_generate(m, "Tell me about the sea.", max_tokens = 20, seed = 1)
)
close(steered)
close(m)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=llm_ablate]{llm_ablate()}}, \code{\link[=llm]{llm()}}, \code{\link[=llm_generate]{llm_generate()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervene.R
\name{llm_ablate}
\alias{llm_ablate}
\title{Ablate neurons in a model}
\usage{
llm_ablate(m, layer, neurons, value = 0, component = "residual")
}
\arguments{
\item{m}{An \code{llm} handle from \code{\link[=llm]{llm()}}.}

\item{layer}{Single 1-based transformer block, in \code{1:m$layers}.}

\item{neurons}{A non-empty vector of 1-based neuron indices in
\code{1:m$hidden_size} to force to \code{value}.}

\item{value}{Single finite number the neurons are forced to (default \code{0}).}

\item{component}{Which sub-layer to ablate. Only \code{"residual"} (the default) is
supported in this release.}
}
\value{
A new \code{llm} handle with the ablation added to its accumulated
\code{interventions}; the source handle is returned unchanged. \code{print()} shows the
active intervention count and \code{summary()} lists them.
}
\description{
Returns a \strong{new} \code{llm} handle with the listed 1-based \code{neurons} of \code{component}
at \code{layer} forced to \code{value} during every subsequent forward pass
(\code{llm_generate()}, \code{llm_logits()}). The original handle is untouched: to remove
the ablation, use the original object (reversibility is exact, D-003).
}
\details{
Ablation forces \code{x[neuron] := value} at the residual choke point -- a native
graph op (\code{x * mask + add}), reversible by construction. Unlike steering it
covers \strong{every} layer, including layer 1.

\strong{Composition.} Deriving from an already-steered or ablated handle carries its
full spec forward and adds the new one, then builds a single fresh context from
the original weights (contexts are never chained). Ablation is a \strong{union,
last-write-wins} per \verb{(layer, neuron)} -- a later ablation of the same neuron
overrides its value. The semantics are \strong{derivation-order-independent}:
\code{m |> llm_ablate(...) |> llm_steer(...)} produces the same forward pass as
\code{m |> llm_steer(...) |> llm_ablate(...)}, and a steer never moves an ablated
neuron (the ablation runs after the steer at the graph, \code{(x + steer) * mask + add}, forcing the neuron to \code{value}, D-016).

\strong{Not free.} Each \code{llm_ablate()}/\code{llm_steer()} call allocates a fresh context
on the shared weights -- a sub-second pause and real memory (its own KV cache),
not a cheap copy.

\strong{Generation/logits only (for now).} Interventions apply to \code{llm_generate()}
and \code{llm_logits()}. \code{\link[=llm_embed]{llm_embed()}} and \code{\link[=llm_trace]{llm_trace()}} on an intervened handle
raise \code{rebirth_error_embed} / \code{rebirth_error_trace} rather than silently
returning base vectors mislabeled as ablated.

Only \code{component = "residual"} ablation is supported in this release (the shared
choke point); \code{"attn_out"}/\code{"mlp_out"} raise \code{rebirth_error_intervention}.

\strong{Model support.} Interventions work on any standard-residual decoder; before the
handle is returned, a runtime probe verifies on \emph{this} model that the ablation
takes effect at each requested layer, raising \code{rebirth_error_intervention} rather
than silently doing nothing. \code{llama} and \code{qwen2} are additionally \emph{behaviorally
validated} (the valence / KL acceptance fixtures); other architectures are enabled
once they pass the probe.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("REBIRTH_TEST_MODEL_QWEN"))) withAutoprint(\{ # examplesIf}
m <- llm(Sys.getenv("REBIRTH_TEST_MODEL_QWEN"))
ablated <- llm_ablate(m, layer = 5, neurons = c(10, 42, 128))
# Composition is order-independent: these two derivations behave identically.
dir <- rep(0.05, m$hidden_size)
a <- m |> llm_ablate(layer = 5, neurons = 10) |> llm_steer(layer = 6, direction = dir)
b <- m |> llm_steer(layer = 6, direction = dir) |> llm_ablate(layer = 5, neurons = 10)
summary(a)
close(a)
close(b)
close(ablated)
close(m)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=llm_steer]{llm_steer()}}, \code{\link[=llm]{llm()}}, \code{\link[=llm_generate]{llm_generate()}}
}

# HF fp32 activation-golden tooling (WP6b) — TEST TOOLING ONLY, never a package
# dependency. This pins the venv that generates + self-checks the real-model
# activation golden under tests/llm-golden/qwen/. It is separate from
# ../requirements.txt (the numpy-only synthetic path, which DELIBERATELY omits
# torch/transformers) and is not referenced by DESCRIPTION or Cargo.toml.
#
# Reproduce the venv (from the repo root):
#   python3 -m venv .hf-golden-venv
#   .hf-golden-venv/bin/pip install -r tests/llm-golden/qwen/requirements-hf.txt
#
# Then (downloads Qwen2.5-0.5B-Instruct once into the HF cache):
#   .hf-golden-venv/bin/python tests/llm-golden/qwen/reference_forward_hf.py
#   .hf-golden-venv/bin/python tests/llm-golden/qwen/reference_forward_hf.py --check
#
# Pinned against Python 3.13. Regeneration is governed by the `golden-update`
# skill; these exact versions are recorded in the golden's manifest.json.

# --- direct ---
torch==2.12.1
transformers==5.13.0
huggingface_hub==1.22.0
numpy==2.5.1

# --- pulled in by transformers (pinned for reproducibility) ---
safetensors==0.8.0
tokenizers==0.22.2
accelerate==1.14.0
